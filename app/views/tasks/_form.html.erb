<%= form_for @task do |task| %> 
<% if @task.errors.any? %>
<%= @task.errors.full_messages.to_sentence %>
<% end %>

<%= task.text_field :name %>
<%= task.submit "Monkey Do", class: "button" %>
<% end %>
